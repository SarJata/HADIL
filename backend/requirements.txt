# HADIL backend runtime dependencies (Render-compatible).
# Unpinned to match the existing project style; do not install from the local venv freeze.
# Windows packaging extras live in backend/requirements-windows.txt and are not
# required to start the cloud service.

# API / server
fastapi
uvicorn
python-multipart
python-dotenv

# Data layer
sqlalchemy
pymysql
psycopg2-binary

# Auth / secrets
PyJWT
cryptography

# LLM providers (OpenAI SDK; Gemini/custom use stdlib urllib)
openai

# Insights / RAG
numpy
pandas
sentence-transformers
faiss-cpu
pypdf
python-docx
